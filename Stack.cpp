#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    // its a last in first out(LIFO) system;
    stack<ll> s; // declaring
    // access element
    s.top();
    // modification
    s.push(1); // insert
    s.pop();   // delete
    s.size();  // size calculation
}